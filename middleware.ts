// Next.js requires this file to be named middleware.ts at the project root.
// The implementation lives in proxy.ts (named proxy.ts for historical reasons).
export { middleware, config } from './proxy';
