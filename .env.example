# Supabase public client settings (required for browser + server session client)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only secret (must never be exposed to browser code)
SUPABASE_SERVICE_ROLE_KEY=

# App config
NEXT_PUBLIC_APP_URL=
NEXT_PUBLIC_TENANT_MODE=path
