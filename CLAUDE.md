# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This repository is the **AML/KYC/CDD Portal** — a compliance portal for Anti-Money Laundering (AML), Know Your Customer (KYC), and Customer Due Diligence (CDD) workflows. The project is in its initial state with no code or tooling configured yet.

## Current State

The repository contains only a README. No build system, package manager, framework, or tooling has been set up. When adding the first code, establish and document:

- The tech stack and package manager
- How to install dependencies, build, lint, and run tests
- Environment variable requirements (create a `.env.example`)
- The chosen architectural patterns (API layer, state management, auth)

Update this CLAUDE.md once those decisions are made.

## Repository

- Remote: `truvis25/aml-kyc-cdd-portal`
- Feature branches should be pushed to `origin` and PRs opened against `main`
